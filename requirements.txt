# Perfil CPU universal. Funciona también en una PC con NVIDIA, pero sin acelerar
# la inferencia. Para CUDA usa: python instalar.py --nvidia
-r requirements-common.txt
onnxruntime>=1.21,<1.27
nudenet==3.4.2
